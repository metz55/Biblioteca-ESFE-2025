@model IEnumerable<Library.DataAccess.Domain.Editorials>

@{
    ViewData["Title"] = "Editoriales";
    int numPage = 1;
    int numRegistros = 0;
    int numRegistrosPorPage = 10;
    int[] tops = { 10, 20, 50, 100, 500, 1000, 10000, -1 };
    int topActual = Convert.ToInt32(ViewBag.Top);
}

<style>
    body {
        overflow-x: hidden;
    }

    .spacer {
        margin-right: 20px;
    }

    #contenedor-principal {
        border-radius: 0.5rem;
    }

    #contenedor-principal {
        border-radius: 0.5rem;
        transition: all 0.3s ease;
    }

        #contenedor-principal:hover {
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
            transform: translateY(-2px);
            border: 1px solid #e5e7eb;
        }

    #btn-create {
        transition: all 0.3s ease;
    }

        #btn-create:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
        }

    .anchoAccionesLibro .btn {
        min-width: 80px;
        margin-right: 5px;
    }

    .anchoAccionesLibro > div {
        display: flex;
        gap: 5px;
    }

    .fade-page {
        opacity: 0;
        transition: opacity 0.5s ease;
    }

        .fade-page.show {
            opacity: 1;
        }

    .pagination-container {
        display: flex;
        justify-content: center;
        margin-top: 15px;
        gap: 5px;
    }

    .page-btn {
        display: inline-block;
        padding: 6px 12px;
        border: 1px solid #d1d5db;
        background-color: #f9fafb;
        color: #374151;
        text-decoration: none;
        border-radius: 5px;
        font-size: 14px;
        transition: all 0.2s ease;
    }

        .page-btn:hover {
            background-color: #e5e7eb;
            color: #111827;
        }

        .page-btn.active {
            background-color: #d1d5db;
            color: #111827;
            font-weight: bold;
            border-color: #9ca3af;
        }

    #suggestionsContainer .suggestion-item:hover {
        background-color: #f0f0f0;
    }

    @@media (max-width: 768px) {
        .form-busqueda {
            flex-direction: column !important;
            align-items: stretch !important;
        }

        .spacer {
            margin-right: 0 !important;
            margin-bottom: 10px !important;
        }

        #btn-create {
            width: 100% !important;
            margin-bottom: 15px;
        }

        .anchoAccionesLibro .btn {
            display: block;
            width: 100%;
            margin-bottom: 5px;
        }

        table.table td,
        table.table th {
            font-size: 14px;
            white-space: nowrap;
        }

        .table-responsive {
            overflow-x: auto;
        }
    }
</style>

<div class="card" id="contenedor-principal">
    <div class="card-body">
        <h1 style="padding:15px; font-family: 'Poppins', sans-serif; font-weight: 600; margin-left: -15px;">Editoriales</h1>

        <div class="row g-2 mb-3 align-items-center">
            <div class="col-12 col-md-auto">
                <a asp-action="Create" class="btn btn-success w-100" id="btn-create">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-plus-square" viewBox="0 0 16 16">
                        <path d="M14 1a1 1 0 0 1 1 1v12a1 1 0 0 1-1 1H2a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1zM2 0a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2z" />
                        <path d="M8 4a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 8 4" />
                    </svg>
                    <i class="bi bi-plus-square"></i> Crear Nuevo
                </a>
            </div>
            <div class="col">
                <form asp-action="Index" class="d-flex form-busqueda flex-wrap">
                    <div class="spacer" style="position: relative;">
                        <input type="text"
                               placeholder="Nombre"
                               class="form-control"
                               name="EDITORIAL_NAME"
                               id="editorialNameInput"
                               autocomplete="off"
                               value="@Context.Request.Query["EDITORIAL_NAME"]" />
                              <div id="suggestionsContainer"
                              style="position: absolute; top: 100%; left: 0; right: 0; background: white; border: 1px solid #ddd; border-top: none; display: none; z-index: 1000; max-height: 200px; overflow-y: auto;"></div>
                    </div>
                    <div class="spacer d-flex align-items-center">
                        <label class="control-label me-2">Top</label>
                        <select name="top_aux" class="form-control">
                            @foreach (var item in tops)
                            {
                                string strItem = item != -1 ? item.ToString() : "Todos";
                                string selected = item == topActual ? "selected" : "";
                                <option value="@item" selected="@selected">@strItem</option>
                            }
                        </select>
                    </div>
                    <div class="spacer">
                        <button id="search-button" type="submit" class="btn btn-secondary">
                            <i class="bi bi-reply-fill"></i> Buscar
                        </button>
                    </div>
                    <input type="hidden" name="page" value="@ViewBag.PaginaActual" />
                </form>
            </div>
        </div>

        <hr />

        <div class="table-responsive fade-page" id="tableContainer">
            <table id="customers" class="table table-striped table-bordered align-middle">
                <thead>
                    <tr>
                        <th class="borIz">@Html.DisplayNameFor(model => model.First().EDITORIAL_ID)</th>
                        <th>@Html.DisplayNameFor(model => model.First().EDITORIAL_NAME)</th>
                        <th class="borDer">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.EDITORIAL_ID</td>
                            <td>@item.EDITORIAL_NAME</td>
                            <td class="anchoAccionesLibro">
                                <a href="@Url.Action("Edit", new { id = item.EDITORIAL_ID })" class="btn btn-info" style="background: #1D4ED8">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-pencil-square" viewBox="0 0 16 16">
                                        <path d="M15.502 1.94a.5.5 0 0 1 0 .706L14.459 3.69l-2-2L13.502.646a.5.5 0 0 1 .707 0l1.293 1.293zm-1.75 2.456-2-2L4.939 9.21a.5.5 0 0 0-.121.196l-.805 2.414a.25.25 0 0 0 .316.316l2.414-.805a.5.5 0 0 0 .196-.12l6.813-6.814z" />
                                        <path fill-rule="evenodd" d="M1 13.5A1.5 1.5 0 0 0 2.5 15h11a1.5 1.5 0 0 0 1.5-1.5v-6a.5.5 0 0 0-1 0v6a.5.5 0 0 1-.5.5h-11a.5.5 0 0 1-.5-.5v-11a.5.5 0 0 1 .5-.5H9a.5.5 0 0 0 0-1H2.5A1.5 1.5 0 0 0 1 2.5z" />
                                    </svg>
                                    Editar
                                </a>
                                @*<a href="@Url.Action("Details", new { id = item.EDITORIAL_ID })" class="btn btn-info">
                                    <i class="bi bi-eye"></i> Detalles
                                </a>*@
                                <a href="javascript:void(0)" onclick="confirmDelete(@item.EDITORIAL_ID)" class="btn btn-danger" style="background-color: #cc2649">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-trash3" viewBox="0 0 16 16">
                                        <path d="M6.5 1h3a.5.5 0 0 1 .5.5v1H6v-1a.5.5 0 0 1 .5-.5M11 2.5v-1A1.5 1.5 0 0 0 9.5 0h-3A1.5 1.5 0 0 0 5 1.5v1H1.5a.5.5 0 0 0 0 1h.538l.853 10.66A2 2 0 0 0 4.885 16h6.23a2 2 0 0 0 1.994-1.84l.853-10.66h.538a.5.5 0 0 0 0-1zm1.958 1-.846 10.58a1 1 0 0 1-.997.92h-6.23a1 1 0 0 1-.997-.92L3.042 3.5zm-7.487 1a.5.5 0 0 1 .528.47l.5 8.5a.5.5 0 0 1-.998.06L5 5.03a.5.5 0 0 1 .47-.53Zm5.058 0a.5.5 0 0 1 .47.53l-.5 8.5a.5.5 0 1 1-.998-.06l.5-8.5a.5.5 0 0 1 .528-.47M8 4.5a.5.5 0 0 1 .5.5v8.5a.5.5 0 0 1-1 0V5a.5.5 0 0 1 .5-.5" />
                                    </svg>
                                    Eliminar
                                </a>
                            </td>
                        </tr>
                        numRegistros++;
                        if (numRegistros == numRegistrosPorPage)
                        {
                            numPage++;
                            numRegistros = 0;
                        }
                    }

                    @if (numRegistros == 0 && numPage > 1)
                    {
                        numPage--;
                    }
                </tbody>
            </table>
        </div>
        @if (ViewBag.TotalPaginas > 1)
        {
            <nav class="pagination-container">
                @if (ViewBag.PaginaActual > 1)
                {
                    <a class="page-btn" asp-action="Index" asp-route-page="@(ViewBag.PaginaActual - 1)" asp-route-EDITORIAL_NAME="@Context.Request.Query["EDITORIAL_NAME"]" asp-route-top_aux="@Context.Request.Query["top_aux"]">Anterior</a>
                }
                @for (int i = 1; i <= ViewBag.TotalPaginas; i++)
                {
                    <a class="page-btn @(i == ViewBag.PaginaActual ? "active" : "")" asp-action="Index" asp-route-page="@i" asp-route-EDITORIAL_NAME="@Context.Request.Query["EDITORIAL_NAME"]" asp-route-top_aux="@Context.Request.Query["top_aux"]">@i</a>
                }
                @if (ViewBag.PaginaActual < ViewBag.TotalPaginas)
                {
                    <a class="page-btn" asp-action="Index" asp-route-page="@(ViewBag.PaginaActual + 1)" asp-route-EDITORIAL_NAME="@Context.Request.Query["EDITORIAL_NAME"]" asp-route-top_aux="@Context.Request.Query["top_aux"]">Siguiente</a>
                }
            </nav>
        }
        <form style="display:none;">
            @Html.AntiForgeryToken()
        </form>
    </div>
</div>
@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        function confirmDelete(editorialId) {
            Swal.fire({
                title: '¿Estás seguro?',
                text: "¡No podrás revertir esto!",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#EF4444',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'Sí, eliminar Editorial',
                cancelButtonText: 'Cancelar'
            }).then((result) => {
                if (result.isConfirmed) {
                    fetch(`/Editorials/Delete/${editorialId}`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                            'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]').value
                        }
                    })
                    .then(response => response.json())
                    .then(data => {
                        if (data.success) {
                            Swal.fire(
                                '¡Editorial Eliminada!',
                                data.message,
                                'success'
                            ).then(() => {
                                location.reload();
                            });
                        } else {
                            Swal.fire(
                                'Error',
                                data.message,
                                'error'
                            );
                        }
                    })
                    .catch(error => {
                        Swal.fire(
                            'Error',
                            'Ocurrió un error al intentar eliminar la Editorial.',
                            'error'
                        );
                    });
                }
            });
        }
    </script>
    <script>
        $(document).ready(function() {
            @if (TempData["EditSuccess"] != null && (bool)TempData["EditSuccess"])
            {
                    <text>
                        Swal.fire({
                            icon: 'success',
                            title: '¡Éxito!',
                            text: 'La Editorial ha sido editada correctamente.',
                            confirmButtonColor: '#3085d6',
                            confirmButtonText: 'Aceptar'
                        });
                    </text>
            }
        });
    </script>
    <script>
        $(document).ready(function() {
            @if (TempData["CreateSuccess"] != null && (bool)TempData["CreateSuccess"])
            {
                    <text>
                        Swal.fire({
                            icon: 'success',
                            title: '¡Éxito!',
                            text: 'La Editorial se creo correctamente.',
                            confirmButtonColor: '#3085d6',
                            confirmButtonText: 'Aceptar'
                        });
                    </text>
            }
        });
    </script>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const container = document.getElementById("tableContainer");
            if (container) {
                setTimeout(() => container.classList.add("show"), 50);
            }
            // Añadir el evento a los botones de paginación
            document.querySelectorAll(".page-btn").forEach(link => {
                link.addEventListener("click", function (e) {
                    e.preventDefault();
                    if (container) {
                        container.classList.remove("show");
                        setTimeout(() => {
                            window.location.href = this.href;
                        }, 500);
                    }
                });
            });
        });
    </script>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const editorialNameInput = document.getElementById("editorialNameInput");
            const suggestionsContainer = document.getElementById("suggestionsContainer");

            editorialNameInput.addEventListener("input", async () => {
                const query = editorialNameInput.value.trim();
                if (query.length < 1 ) {
                    suggestionsContainer.style.display = "none";
                    return;
                }

                try {
                    const response = await fetch(`/Editorials/Search?nombre=${encodeURIComponent(query)}`);
                    const suggestions = await response.json();

                    if (suggestions.length > 0) {
                        suggestionsContainer.innerHTML = suggestions
                            .map(suggestion => `<div class="suggestion-item" style="padding: 8px; cursor: pointer;" onclick="selectSuggestion('${suggestion.editorialName}')">${suggestion.editorialName}</div>`)
                            .join("");
                        suggestionsContainer.style.display = "block";
                    } else {
                        suggestionsContainer.style.display = "none";
                    }
                } catch (error) {
                    console.error("Error al obtener sugerencias:", error);
                    suggestionsContainer.style.display = "none";
                }
            });

            // Ocultar sugerencias si se hace clic fuera del campo
            document.addEventListener("click", (e) => {
                if (e.target !== editorialNameInput) {
                    suggestionsContainer.style.display = "none";
                }
            });
        });

        function selectSuggestion(suggestion) {
            document.getElementById("editorialNameInput").value = suggestion;
            document.getElementById("suggestionsContainer").style.display = "none";
        }
    </script>

}



