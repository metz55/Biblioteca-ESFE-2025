@model Library.DataAccess.Domain.Countries

@{
    ViewData["Title"] = "Eliminar País";
}

<style>
    body {
        overflow-x: hidden;
        background-color: #f8f9fa;
    }

    .delete-card {
        background: white;
        border-radius: 15px;
        box-shadow: 0 0 10px rgba(0,0,0,0.1);
        padding: 30px;
        margin-top: 40px;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
    }

    dt {
        color: #6c757d;
        text-align: right;
    }

    dd {
        margin-bottom: 10px;
    }

    .btn-group-custom {
        display: flex;
        justify-content: center;
        gap: 15px;
        margin-top: 30px;
    }
</style>

<div class="container">
    <div class="delete-card">
        <h3 class="text-center text-danger">🗑️ ¿Estás seguro de eliminar el país?</h3>
        <hr />
        <dl class="row">
            <dt class="col-sm-4">ID:</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.COUNTRY_ID)</dd>

            <dt class="col-sm-4">Nombre:</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.COUNTRY_NAME)</dd>
        </dl>

        <form asp-action="Delete" class="btn-group-custom">
            <input type="hidden" asp-for="COUNTRY_ID" />
            <button type="submit" class="btn btn-danger d-flex align-items-center">
                <i class="bi bi-trash3 me-2"></i> Eliminar
            </button>
            <a asp-action="Index" class="btn btn-secondary d-flex align-items-center">
                <i class="bi bi-reply-fill me-2"></i> Cancelar
            </a>
        </form>
    </div>
</div>

@section Scripts {
    <script>
        document.querySelector('.main-content')?.classList.remove('with-menu');
        document.querySelector('.main-content')?.classList.add('full-width');
    </script>
}
