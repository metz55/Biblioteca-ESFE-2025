@model Library.DataAccess.Domain.AcquisitionTypes

@{
    ViewData["Title"] = "Editar Tipo de Adquisición";
}

<style>
    .edit-card {
        background: white;
        border-radius: 15px;
        box-shadow: 0 0 15px rgba(0,0,0,0.1);
        padding: 30px;
        margin: 40px auto;
        max-width: 800px;
    }

        .edit-card h3 {
            text-align: center;
            color: #ffc107;
            font-weight: bold;
            margin-bottom: 25px;
        }

    .form-table {
        width: 100%;
        border-collapse: collapse;
    }

        .form-table th,
        .form-table td {
            padding: 12px;
            border: 1px solid #dee2e6;
            vertical-align: middle;
        }

        .form-table th {
            background-color: #f8f9fa;
            text-align: right;
            width: 30%;
        }

    @@media (max-width: 768px) {
        .form-table th, .form-table td

    {
        display: block;
        width: 100%;
    }

    .form-table th {
        text-align: left;
        background-color: transparent;
        font-weight: bold;
        margin-top: 15px;
    }

    .btn-group {
        flex-direction: column;
    }

    }

    .btn svg {
        margin-right: 6px;
    }
</style>

<div class="edit-card">
    <h3>📝 Editar Tipo de Adquisición</h3>

    <form asp-action="Edit">
        <input type="hidden" asp-for="ACQUISITION_ID" />
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <table class="form-table">
            <tr>
                <th><label asp-for="ACQUISITION_TYPE"></label></th>
                <td>
                    <input asp-for="ACQUISITION_TYPE" class="form-control" />
                    <span asp-validation-for="ACQUISITION_TYPE" class="text-danger"></span>
                </td>
            </tr>
        </table>

        <div class="text-center mt-4 d-flex justify-content-center gap-3 flex-wrap btn-group">
            <button type="submit" class="btn btn-success">
                <i class="bi bi-floppy"></i> Guardar
            </button>
            <a asp-action="Index" class="btn btn-secondary">
                <i class="bi bi-reply-fill"></i> Regresar
            </a>
        </div>
    </form>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
    <script>
        document.querySelector('.main-content')?.classList.remove('with-menu');
        document.querySelector('.main-content')?.classList.add('full-width');
    </script>
}
